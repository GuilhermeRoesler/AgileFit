# URL pública do site (OG tags, canonical, sitemap e robots no build)
VITE_SITE_URL=https://example.com

# Endpoint de inscrição de leads (POST JSON: { ok, message? })
VITE_LEADS_API_URL=https://example.com/api/leads
